// Not built, and blocked on the backend decision in docs/ROADMAP.md: the
// OAuth handshake and token store cannot live in a static client.
export default {
  id: 'ecommerce',
  nav: [],
  routes: {},
  mount() {},
  unmount() {},
};
